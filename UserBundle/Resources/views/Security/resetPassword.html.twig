{% extends '@CleverAgeEAVManagerUser/base.html.twig' %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{{ 'reset_password.title'|trans({}, 'security') }}</h3>
        </div>
        <div class="panel-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <p><a href="{{ path('login') }}">{{ 'reset_password.back'|trans({}, 'security') }}</a></p>
            <input class="btn btn-lg btn-success btn-block" type="submit" id="_submit" name="_submit"
                   value="{{ 'reset_password.submit'|trans({}, 'security') }}">
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
